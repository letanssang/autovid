Write 3 short, punchy YouTube thumbnail text variants for an educational video on "{{ topic }}".

Opening hook narration (for context on the video's angle):
{{ hook_text }}

Each variant must be:
- At most 6 words — thumbnail text must be readable at a glance, at small size.
- High-impact and curiosity-driving: a bold claim, a surprising number, or a sharp question. Not a
  generic label like "{{ topic }}" or "Introduction to {{ topic }}".
- Plain text, no ALL CAPS, no excessive punctuation or emoji.

Respond with a JSON array of exactly 3 strings, e.g. ["Variant A text", "Variant B text", "Variant C text"].
No other text.
