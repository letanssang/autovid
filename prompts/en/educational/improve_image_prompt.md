Rewrite the following AI image-generation prompt per the instruction below. This
prompt will be handed to a human, who will use it with any free image tool of
their choice to create an illustrative image for a YouTube video beat.

Current prompt:
"{{ current_prompt }}"

Instruction:
{{ instruction }}

Respond with ONLY the rewritten prompt text — no preamble, no explanation, no
quotes, no markdown formatting.
