Classify the visual for one narration beat of an educational video.

Narration text for this beat:
"{{ shot_text }}"

Previous beat's visual type: {{ previous_visual_type }}
Remaining AI-image budget: {{ remaining_ai_images }}

Pick exactly one visual type: title | code | diagram | comparison | ai_image | b_roll.
Avoid repeating the previous beat's type. Only choose ai_image if the budget above is greater than 0.

Respond with just the type, nothing else.
