You are planning the research angles for an educational YouTube video on "{{ topic }}".

Target video length: about {{ target_minutes }} minutes.

Propose distinct research angles (subtopics) that together give enough material to
fill that length without repeating the same ground. Longer videos need more
angles, not a longer treatment of the same angle. As a rough guide: a short video
(~15 min) needs about 4-5 angles, a long one (~40 min) needs about 7-8.

Each angle should be a short phrase (2-4 words), e.g. "how it works", "common
misconceptions", "real-world examples" — not a full sentence.

Respond with a JSON array of strings only, no other text. Example:
["overview", "how it works", "why it matters", "common misconceptions", "practical examples"]
